{
  "name": "@milo/root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  
  "scripts": {
    "mobile": "yarn workspace @milo/mobile expo start",
    "web": "yarn workspace @milo/web expo start --web",
    "postinstall": "expo-yarn-workspaces check-workspace-dependencies"
  },
  "devDependencies": {
    "expo-yarn-workspaces": "^1.5.0"
  }
}
